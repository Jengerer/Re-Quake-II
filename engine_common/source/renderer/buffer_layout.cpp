#include "renderer/buffer_layout.h"

namespace Renderer
{

	BufferLayout::BufferLayout(const Attribute *attributes, int attributeCount) : attributes(attributes), attributeCount(attributeCount)
	{
	}

}